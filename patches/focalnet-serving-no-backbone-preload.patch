diff --git a/models/dino/backbone.py b/models/dino/backbone.py
--- a/models/dino/backbone.py
+++ b/models/dino/backbone.py
@@ -190,7 +190,7 @@ def build_backbone(args):
             'swin_L_384_22k': 'swin_large_patch4_window12_384_22k.pth',
         }
         pretrainedpath = os.path.join(pretrained_dir, PTDICT[args.backbone])
-        checkpoint = torch.load(pretrainedpath, map_location='cpu')['model']
+        checkpoint = torch.load(pretrainedpath, map_location='cpu')['model']  # Serving: retain Swin preload.
         from collections import OrderedDict
         def key_select_function(keyname):
             if 'head' in keyname:
@@ -236,6 +236,6 @@ def build_backbone(args):
         }
         pretrainedpath = os.path.join(pretrained_dir, PTDICT[args.backbone])
-        checkpoint = torch.load(pretrainedpath, map_location='cpu')['model']
+        checkpoint = {}  # Serving: the full task checkpoint is strict-loaded later.
         from collections import OrderedDict
         def key_select_function(keyname):
             if 'head' in keyname:
